# Production site URL (no trailing slash). Used for canonical URLs, Open Graph, and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://derrolsislandtours.com

# Optional: override footer “Tera” link (default in code: https://teralatam.co). No trailing slash.
# NEXT_PUBLIC_TERA_URL=https://teralatam.co
